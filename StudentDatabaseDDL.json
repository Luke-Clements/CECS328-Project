{"SQLTableDDL":["CREATE TABLE Assignment (cID INT NOT NULL, aName VARCHAR(20) NOT NULL, aCategory VARCHAR(15) NOT NULL, score FLOAT, maxScore INT NOT NULL, PRIMARY KEY(cID, aName, aCategory))",

"CREATE TABLE Class (cID INT NOT NULL, cName VARCHAR(15) NOT NULL, teacherName VARCHAR(30), semester VARCHAR(10) NOT NULL, schoolName VARCHAR(20) NOT NULL, cTime VARCHAR(7) NOT NULL, gsID INT NOT NULL, PRIMARY KEY(cID))",

"CREATE TABLE GradingScale (gsID INTEGER NOT NULL, A FLOAT NOT NULL, B FLOAT NOT NULL, C FLOAT NOT NULL, D FLOAT NOT NULL, F FLOAT NOT NULL, passFail BOOLEAN NOT NULL, PRIMARY KEY(gsID))",

"CREATE TABLE CategoryWeight (cwCategory VARCHAR(15) NOT NULL, weight INTEGER NOT NULL, classID INT NOT NULL, PRIMARY Key(cwCategory, weight, classID))",

"ALTER TABLE Assignment ADD CONSTRAINT FK_AssignmentClass FOREIGN KEY (cID) REFERENCES Class(cID)",

"ALTER TABLE Class ADD CONSTRAINT FK_ClassGradingScale FOREIGN KEY (gsID) REFERENCES GradingScale(gsID)",

"ALTER TABLE CategoryWeight ADD CONSTRAINT FK_ClassCategoryWeight FOREIGN KEY (classID) REFERENCES Class(cID)"]}
